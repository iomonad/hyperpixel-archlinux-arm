# hyperpixel-archlinux-arm
A package that aims to remplace Pimoroni's setup script for ARM Archlinux platform 

## Install

```
git clone https://github.com/iomonad/hyperpixel-archlinux-arm.git
cd hyperpixel-archlinux-arm
makepkg -si
```
